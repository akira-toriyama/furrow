# a draft with nothing set — the minimal shard

Carries no unknown keys, so it must marshal byte-identically to what every
furrow since v0.9.0 wrote: the passthrough must not disturb a shard that has
nothing to pass through.
